/**
 * Public entry point for BreakingNewsTicker.
 *
 * All consumers import from `@/components/BreakingNewsTicker` — this barrel
 * re-exports the default component so every existing import path keeps working.
 */
export { default } from "./BreakingNewsTicker";
export type { NewsItem, TickerConfig } from "./types";
