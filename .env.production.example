# ====================================
# PRODUCTION ENVIRONMENT VARIABLES
# ====================================
# These values should be set in your deployment platform (Vercel, AWS, etc.)
# DO NOT commit actual production secrets to git

# --- CASINO CONFIGURATION (PRODUCTION) ---
# Point to your deployed casino backend (NOT localhost!)
# This URL will be loaded directly in the iframe on the client side

# Option 1: Casino on .shop domain (recommended - same main domain)
NEXT_PUBLIC_CASINO_URL=https://www.bullmoney.shop/demogames

# Option 2: Casino on .online domain (if deployed there)
# NEXT_PUBLIC_CASINO_URL=https://www.bullmoney.online/demogames

# Option 3: Casino on subdomain
# NEXT_PUBLIC_CASINO_URL=https://casino.bullmoney.shop

# Production doesn't need CASINO_BACKEND_URL - no proxy used
# The iframe loads NEXT_PUBLIC_CASINO_URL directly

# --- EXAMPLE: If you deploy casino backend separately ---
# NEXT_PUBLIC_CASINO_URL=https://casino.yourdomain.com
# or
# NEXT_PUBLIC_CASINO_URL=https://yourdomain.com/casino

# --- OTHER PRODUCTION ENV VARS ---
# (Copy from .env.example and set production values)
NEXT_PUBLIC_BASE_URL=https://www.bullmoney.shop
NODE_ENV=production

# Supabase (production)
NEXT_PUBLIC_SUPABASE_URL=https://your-prod-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_production_anon_key

# Database (production)
DATABASE_URL=your_production_database_url

# Stripe (production keys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your_live_key
STRIPE_SECRET_KEY=sk_live_your_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ... (other production variables)
