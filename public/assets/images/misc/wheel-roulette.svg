<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200">
  <defs>
    <radialGradient id="rg" cx="50%" cy="40%">
      <stop offset="0%" stop-color="#fff" stop-opacity="0.06"/>
      <stop offset="100%" stop-color="#000" stop-opacity="0.0"/>
    </radialGradient>
  </defs>
  <circle cx="100" cy="100" r="96" fill="#0f212e" stroke="rgba(255,255,255,0.06)" stroke-width="4"/>
  <g transform="translate(100,100)">
    <!-- simple segments -->
    ${Array.from({length:12}).map((_,i)=>{
      const a = (i*30-90) * Math.PI/180;
      const x1 = 96*Math.cos(a), y1 = 96*Math.sin(a);
      const a2 = ((i+1)*30-90) * Math.PI/180;
      const x2 = 96*Math.cos(a2), y2 = 96*Math.sin(a2);
      const color = i%3===0? '#ef4444' : i%3===1? '#f59e0b' : '#535353';
      return `<path d="M0 0 L ${x1} ${y1} A 96 96 0 0 1 ${x2} ${y2} Z" fill="${color}" stroke="#00000022"/>`;
    }).join('')}
  </g>
  <circle cx="100" cy="100" r="36" fill="#0f212e" stroke="rgba(255,255,255,0.08)"/>
  <circle cx="100" cy="100" r="8" fill="#00e701"/>
</svg>
