#!/usr/bin/env bash
# ═══════════════════════════════════════════════════════════════
#  🐂 BULLMONEY CLEAN  —  Universal launcher (Mac / Linux)
#  
#  INSTALL (one-time):
#    sudo ln -sf "$(pwd)/scripts/bullclean" /usr/local/bin/bullclean
#  
#  USAGE:
#    bullclean          ← fix & clean everything
#    bullclean --dry    ← preview only
#    bullclean --deploy ← full deploy check
#    bullclean --popup  ← open in native Terminal.app
# ═══════════════════════════════════════════════════════════════
set -e

# Find the project root (where this script lives → parent dir)
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"

cd "$PROJECT_ROOT"

# Auto-detect python3 — prefer venv if present
if [ -f "$PROJECT_ROOT/.venv/bin/python3" ]; then
    PYTHON="$PROJECT_ROOT/.venv/bin/python3"
elif command -v python3 &>/dev/null; then
    PYTHON="python3"
elif command -v python &>/dev/null; then
    PYTHON="python"
else
    echo "❌  Python not found. Install Python 3.8+ first."
    exit 1
fi

exec "$PYTHON" "$SCRIPT_DIR/clean.py" "$@"
